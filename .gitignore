node_modules/
.next/
out/
build/
.env
.env.local
.env*.local
next-env.d.ts
*.tsbuildinfo
.DS_Store

# 로컬 자격증명 (프로젝트에 이미 있던 파일 포함)
.db.env
*.env
!.env.example

# 기관 설정값 등 로컬 전용 메모
*.local.md

# 내부용 촘촘한 판(k=2). 공개 저장소에는 k=3 판만 싣는다
public/universe-internal.*
